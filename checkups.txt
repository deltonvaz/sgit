//    val f = File(newBlobPath)
//    f.createIfNotExists()
//      .appendLine(headerLine)
//      .appendLine(content)

    //Builder
    //valor de parametro default
    //conversion implicits
    //classes implicites
    // GIVEN - WHEN - THEN
    // --teste
    //exemplo de given:
    /*
      given(um bagulho loco)
      then(um bagulho loco deve acontecer)

      result =

      exemplo:
      given an empty array
      val a = anEmptyArray()

      then add 0 to a
        a :+ 1 --concatena 1 ao array
     */

